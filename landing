<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Axiscorps - Reduce No-Shows, Maximize Sales</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        * { margin:0; padding:0; box-sizing:border-box; }
        body { font-family:'Inter',sans-serif; background:#001F3F; color:#fff; line-height:1.6; }
        .hero { background:linear-gradient(135deg,#001F3F 0%,#4682B4 100%); padding:120px 20px; text-align:center; }
        .hero h1 { font-size:clamp(2.5rem,5vw,4rem); margin-bottom:1rem; }
        .hero p { font-size:1.3rem; max-width:600px; margin:0 auto 2rem; }
        .cta { background:#4682B4; color:#fff; padding:18px 40px; font-size:1.2rem; font-weight:600; border:none; border-radius:50px; cursor:pointer; transition:transform 0.3s, box-shadow 0.3s; text-decoration:none; display:inline-block; }
        .cta:hover { transform:translateY(-3px); box-shadow:0 10px 30px rgba(70,130,180,0.5); }
        .services { padding:80px 20px; max-width:1200px; margin:auto; }
        .services h2 { text-align:center; font-size:2.5rem; margin-bottom:4rem; }
        .grid { display:grid; grid-template-columns:repeat(auto-fit,minmax(300px,1fr)); gap:30px; }
        .card { background:rgba(70,130,180,0.1); border:2px solid #4682B4; border-radius:15px; padding:40px 30px; text-align:center; transition:transform 0.3s; }
        .card:hover { transform:translateY(-10px); background:rgba(70,130,180,0.2); }
        .card h3 { font-size:1.8rem; margin-bottom:1rem; color:#4682B4; }
        .stats { background:#4682B4; padding:60px 20px; text-align:center; }
        .stats h2 { font-size:2.5rem; margin-bottom:2rem; }
        .stat { display:inline-block; margin:0 2rem; font-size:2rem; font-weight:700; }
        footer { background:#001F3F; padding:40px 20px; text-align:center; }
        @media (max-width:768px) { .hero { padding:80px 20px; } .grid { grid-template-columns:1fr; } }
    </style>
</head>
<body>
    <section class="hero">
        <h1>Axiscorps</h1>
        <p>Powerful agency solutions to reduce no-shows by 40%+ and skyrocket your sales with smart automation.</p>
        <a href="#contact" class="cta">Start Winning Today</a>
    </section>
    <section class="services" id="services">
        <h2>Our Core Solutions</h2>
        <div class="grid">
            <div class="card">
                <h3>Smart Reminders</h3>
                <p>Automated SMS/Email alerts that slash no-shows and keep clients committed.</p>
            </div>
            <div class="card">
                <h3>Sales Funnels</h3>
                <p>AI-optimized upsells and booking flows that boost revenue per customer.</p>
            </div>
            <div class="card">
                <h3>Agency Analytics</h3>
                <p>Real-time dashboards to track performance and refine strategies.</p>
            </div>
        </div>
    </section>
    <section class="stats">
        <h2>Proven Results</h2>
        <div class="stat">40% ↓ No-Shows</div>
        <div class="stat">3x ↑ Sales</div>
        <div class="stat">24/7 Automation</div>
    </section>
    <footer id="contact">
        <p>&copy; 2026 Axiscorps. Ready to transform your agency? <a href="mailto:hello@axiscorps.tech" style="color:#4682B4;">hello@axiscorps.tech</a></p>
    </footer>
</body>
</html>
